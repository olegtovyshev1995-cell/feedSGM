# ── Runtime ───────────────────────────────────────────────────────
pydantic>=2.6,<3            # валидация конфига на старте (Pydantic v2)
PyYAML>=6.0                 # чтение config.yaml
google-api-python-client>=2.120  # Google Sheets API v4
google-auth>=2.28          # авторизация сервисного аккаунта (read-only)

# ── Dev / CI ──────────────────────────────────────────────────────
pytest>=8.0                # тесты валидации и маппинга
